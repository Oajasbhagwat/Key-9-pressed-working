   #include "A4_Test.h"
#include "Debug.h"
//#include <stdlib.h>
#include <math.h>
#include <arduino.h>

  #include <avr/pgmspace.h>
 //----------------------------------------------------------------
   //#ifndef __A4_Test_H
  //#define __A4_Test_H
   //#endif
#include "DEV_Config.h"


#include "LCD_Driver.h"
#include "fonts.h"
 #include "LCD_GUI.h"
   
    
 //-----------------------------------------------------------/
   //unsigned int x1=20,x2=100,x3,x4, x6,x7,x8,x9,x10, y1=50,y2=50,y3,y4,y5, y6,y7,y8,y9,y10 ; // for drawing lines & Rectangles
  //volatile String Str1,Str2,Str3,Str4,Str5 ; // for showing 'float' values 
   unsigned int   Backg1=0xFFFF ;
   //---------------------------------------------------------
    //------------------------------------------------------------------------------------
    void A4_ckt1(void);  // prototype definition
      extern unsigned int ya13;
       // void GUI_DrawLine(POINT Xstart, POINT Ystart, POINT Xend, POINT Yend, COLOR Color, LINE_STYLE Line_Style, DOT_PIXEL Dot_Pixel)
    //    Draw lines, Rectangles, circles
    //-------------------------------------------------
    void A4_ckt1()
    {  // y13=250;
       //GUI_DrawLine(50,ya13,90, ya13, BROWN, LINE_SOLID, 2 );
         //GUI_DrawLine(0, 300, LCD_WIDTH, 300, RED, LINE_DOTTED, DOT_PIXEL_DFT);
         //GUI_DrawLine(0, 310, LCD_WIDTH, 310, GREEN, LINE_SOLID, DOT_PIXEL_2X2);
    
    
    
      // void GUI_DrawLine(POINT Xstart, POINT Ystart, POINT Xend, POINT Yend,   COLOR Color, LINE_STYLE Line_Style, DOT_PIXEL Dot_Pixel)
    }
    //  --------------------------------------------------------------------------------
